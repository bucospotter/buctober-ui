import React from "react"
import Layout from "../components/Layout"
import { Link } from "gatsby"

export default function Contact() {
  return(
  <Layout>
    <h1>Hello people from contact page</h1>
  </Layout>
  )}
